# -*- coding: utf-8 -*-
#
# Copyright (C) 2023-2026 Northwestern University.
#

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason'
        required: false
        default: 'Manual trigger'

jobs:
  tests:
    uses: galterlibrary/reusable-workflows/.github/workflows/inveniordm-python-run-tests.yml@main
